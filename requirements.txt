# Basic dependencies (CPU + MPI)
numpy>=1.23
mpi4py>=3.1

# For GPU version, install CuPy matching your CUDA version, for example:
#   pip install cupy-cuda11x   # for CUDA 11.x
#   pip install cupy-cuda12x   # for CUDA 12.x
